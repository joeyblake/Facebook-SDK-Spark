<?php

# Load facebook library
$autoload['libraries'] = array('src/facebook');

# Load the facebook config when the spark is loaded
$autoload['config'] = array('facebook_config');

# Load the facebook helper when the spark is loaded
//$autoload['helper'] = array('facebook_config');
